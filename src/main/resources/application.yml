server:
  port: 6543

spring:
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    pool:
      max-active: 8
      min-idle: 0
      max-wait: -1
      max-idle: 8
  session:
    store-type: none
#Logback
logging:
  config: classpath:xml/log/logback-boot.xml

